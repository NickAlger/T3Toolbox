UniformGaussNewtonModel.evaluate
================================

.. py:method:: t3toolbox.fitting.UniformGaussNewtonModel.evaluate(p)

   The quadratic-model value ``m(p) = c + gᵀp + ½ pᵀHp``, reusing ``c`` / ``g`` and ONE forward apply.

